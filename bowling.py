import os

class Game(object):
    """This is the main, and only, class in the package."""
    def __init__(self):
        super(Game, self).__init__()
        self.rolls = ()

    def roll(pins):
        print "Entering roll()"

    def score():
        print "Entering score()"
        return 0
